---
 - name: printing the numbers by using loop
   hosts: localhost
   gather_facts: ture
   tasks:
     - name: Echo the value
       command: echo " {{ item }}"
       loop:
         - 10
         - 11
         - 12
         - 13
         - 14
         - 15
         
